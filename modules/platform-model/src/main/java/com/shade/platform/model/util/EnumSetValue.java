package com.shade.platform.model.util;

/**
 * An interface that represents an enum with associated bit
 * mask value that can safely be {@code OR}-ed together.
 */
public interface EnumSetValue extends EnumValue {
}
